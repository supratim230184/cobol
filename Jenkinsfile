pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                if (${branch==master))
                echo 'Hello World'
                      fi
                if ((${branch==de))  
                      echo "hello dev"
                      fi
            }
        }
    }
}

